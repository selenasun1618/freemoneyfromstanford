import { theme } from "../constants/theme"

export function MaximilienCura() {
    return (
        <a className={`underline ${theme.linkColor}`}>Maximilien Cura</a>
    )
}

export function SelenaSun() {
    return (
        <a className={`underline ${theme.linkColor}`}>Selena Sun</a>
    )
}